(jbuild_version 1)

(executables
 ((libraries (bitbucket qcheck core))
  (modules (test))
  (names (test))))

(alias
 ((name runtest)
  (package bitbucket)
  (deps (test.exe))
  (action (run ${<}))))
